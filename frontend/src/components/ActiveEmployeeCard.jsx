function ActiveEmployeeCard({name, surname, workStartTime}) {
    return (
        <div>
            <div></div>
            <p>{name} {surname}</p>
            <p>{workStartTime}</p>
        </div>
    )
}

export default ActiveEmployeeCard